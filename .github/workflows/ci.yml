name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore backend/SchoolMS.sln

    - name: Build
      run: dotnet build backend/SchoolMS.sln --no-restore

    - name: Test
      run: dotnet test backend/SchoolMS.sln --no-build --verbosity normal